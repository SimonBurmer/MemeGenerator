const generatedMeme = class {
  constructor(title = "Title", description="Description", meme) {
    this.title = title;
    this.description = description;
    this.meme = meme;
  }
};

export default generatedMeme;